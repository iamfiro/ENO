export interface Music {
    name: {
        kr: string,
        en: string,
    };
    artist: string;
    filePath: string;
    coverPath: string;
}

export interface ArtistType {
    name: string;
    image: string;
}